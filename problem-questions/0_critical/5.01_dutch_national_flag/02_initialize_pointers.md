# Initialize Pointers

**Q:** What are the initial values for smaller, equal, and larger?

**A:**
- `smaller = 0` (middle region starts empty at beginning)
- `equal = 0` (first element is unprocessed)
- `larger = len(A)` (top region starts empty, built from right)
