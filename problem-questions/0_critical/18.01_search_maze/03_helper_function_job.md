# Helper Function's Job

**Q:** In recursive DFS path-finding, what is the helper function's job?

**A:** Determine if there's a path from current to goal. Return True if found, False otherwise. Build the path as a side effect.
