# Helper Signature

**Q:** What are the arguments and return type for a recursive DFS path-finding helper?

**A:**
- **Args**: current node (path and visited are accessed via closure)
- **Returns**: bool — True if path to goal exists from current
