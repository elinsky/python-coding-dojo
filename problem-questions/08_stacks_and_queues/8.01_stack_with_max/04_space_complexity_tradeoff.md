# Space Complexity Tradeoff

**Q:** What's the space complexity tradeoff for O(1) max() in a stack?

**A:** Worst case O(n) extra space if all elements are increasing. Best case O(1) if all elements are the same or decreasing.
