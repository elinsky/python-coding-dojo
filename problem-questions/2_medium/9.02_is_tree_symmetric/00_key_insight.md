# Key Insight

**Q:** What's the key insight for checking if a binary tree is symmetric?

**A:** Walk two paths simultaneously, comparing mirror positions:
- left.left must match right.right
- left.right must match right.left
