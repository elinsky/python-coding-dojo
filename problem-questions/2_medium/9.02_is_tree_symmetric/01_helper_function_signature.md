# Helper Function Signature

**Q:** What arguments does the helper function take for is_symmetric, and what does it return?

**A:** Takes two nodes: `helper(left, right)`. Returns `bool` - True if the two subtrees are mirrors of each other.
