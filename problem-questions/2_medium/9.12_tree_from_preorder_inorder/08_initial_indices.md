# Initial Indices

**Q:** What are the initial values for the 4 indices when starting the recursion?

**A:**
- `preorder_start = 0`
- `preorder_end = len(preorder)`
- `inorder_start = 0`
- `inorder_end = len(inorder)`

The full arrays.
