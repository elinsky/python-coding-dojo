# Partition Around Pivot: Key Variables

**Q:** What are the key variables used in `partition_around_pivot`, and what do they represent?

**A:**
- `pivot_value`: The value of the pivot element used for comparison
- `new_pivot_idx`: Marks the boundary between elements satisfying `comp` and those that do not
- `left, right`: Define the current subarray being partitioned
