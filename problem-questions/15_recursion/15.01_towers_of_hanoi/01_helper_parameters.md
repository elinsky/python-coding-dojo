# Helper Function Parameters

**Q:** What 4 parameters does the recursive helper need?

**A:**
1. `num_rings` - how many rings to move
2. `from_peg` - source peg
3. `to_peg` - destination peg
4. `aux_peg` - auxiliary peg to use as intermediate
