# Why Increment Both in < Pivot Case

**Q:** Why increment both pointers in the < pivot case?

**A:** Element belongs in bottom region. After swap, the element at equal is now from middle region (safe to advance).
