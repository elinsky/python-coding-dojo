# Dictionary Get with Default Value

**Q:** How do I get a value from a dictionary with a default if key doesn't exist? What is the time complexity?

**A:** Use get() method. Time: O(1) average

```python
value = d.get(key, default_value)
```
