# Set Difference

**Q:** How do I get elements in set s that are not in set t? What is the time complexity?

**A:** Use - operator or difference() method. Time: O(len(s)) average

```python
diff = s - t
diff = s.difference(t)
```
