# Pop Arbitrary Element from Set

**Q:** How do I remove and return an arbitrary element from a set? What is the time complexity?

**A:** Use pop() method (raises KeyError if empty). Time: O(1) average

```python
element = s.pop()
```
