# Check if Vertex Visited

**Q:** How do I check if a vertex has been visited?

**A:** Use the 'in' operator with the visited set. Time: O(1) average

```python
if v in visited:
    # already visited
```
