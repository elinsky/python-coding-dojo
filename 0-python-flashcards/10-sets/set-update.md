# Update Set with Elements

**Q:** How do I add multiple elements to a set? What is the time complexity?

**A:** Use update() method. Time: O(len(other))

```python
s.update([1, 2, 3])
s.update(other_set)
```
