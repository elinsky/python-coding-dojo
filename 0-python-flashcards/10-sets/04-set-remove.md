# Remove Element from Set (with error)

**Q:** How do I remove an element from a set (error if not found)?

**A:** Use remove() method (raises KeyError)

```python
s.remove(x)  # Raises KeyError if not found
```
