# Check if Superset

**Q:** How do I check if set s contains all elements of set t?

**A:** Use >= operator or issuperset() method

```python
if s >= t:
    # s is superset of t
if s.issuperset(t):
    # s is superset of t
```
