# Set Union

**Q:** How do I get all elements from two sets? What is the time complexity?

**A:** Use | operator or union() method. Time: O(len(s) + len(t)) average

```python
combined = s | t
combined = s.union(t)
```
