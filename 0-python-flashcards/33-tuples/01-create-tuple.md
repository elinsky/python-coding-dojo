# Create Tuple

**Q:** How do I create a tuple?

**A:** Use parentheses or tuple() constructor

```python
t = ()  # Empty tuple
t = (1,)  # Single element - comma required!
t = (1, 2, 3)
t = 1, 2, 3  # Parentheses optional
t = tuple([1, 2, 3])
```
