# BST Property

**Q:** What is the BST property?

**A:** Key at node >= all keys in left subtree and <= all keys in right subtree

```python
# For each node:
# - All keys in left subtree <= node.data
# - All keys in right subtree >= node.data
# This is a GLOBAL property across entire tree
```
