# Create Dictionary

**Q:** How do I create a dictionary?

**A:** Use curly braces, dict(), or dict comprehension

```python
d = {}
d = dict()
d = {'a': 1, 'b': 2}
d = dict(a=1, b=2)
d = {k: v for k, v in pairs}
```
