# Check if String Starts With Prefix

**Q:** How do I check if string s starts with a given prefix?

**A:** Use startswith() method - O(k) time where k=len(prefix)

```python
s.startswith(prefix)
```
