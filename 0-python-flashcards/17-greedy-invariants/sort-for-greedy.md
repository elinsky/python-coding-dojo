# Sorting for Greedy

**Q:** What preprocessing step is common in greedy algorithms?

**A:** Sorting the input - O(n log n) time complexity.

```python
A.sort()  # Sort ascending
A.sort(reverse=True)  # Sort descending
A.sort(key=lambda x: x[1])  # Sort by specific attribute
```
