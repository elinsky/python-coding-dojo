# Check if Same Node (Identity)

**Q:** How do I check if two nodes are the same (not just equal data)? What is the time complexity?

**A:** Use identity comparison with is. Time: O(1)

```python
if node1 is node2:
```
