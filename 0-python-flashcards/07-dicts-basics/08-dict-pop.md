# Dictionary Pop

**Q:** How do I remove and return a value by key from a dictionary?

**A:** Use pop() method

```python
value = d.pop(key)  # Raises KeyError if not found
value = d.pop(key, default)  # Returns default if not found
```
