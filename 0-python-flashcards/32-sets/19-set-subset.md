# Check if Subset

**Q:** How do I check if set s is a subset of set t?

**A:** Use <= operator or issubset() method

```python
if s <= t:
    # s is subset of t
if s.issubset(t):
    # s is subset of t
```
