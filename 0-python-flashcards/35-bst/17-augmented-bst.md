# Augmented BST

**Q:** What is an augmented BST?

**A:** A BST with extra fields in nodes for efficient queries. Example: storing subtree size at each node to support O(log n) "count of elements in range" queries.
