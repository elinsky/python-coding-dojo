# Pop Min/Max from bintrees

**Q:** How do I remove and return min/max from bintrees BST?

**A:** Use pop_min()/pop_max()

```python
min_pair = t.pop_min()  # Returns and removes (key, value)
max_pair = t.pop_max()  # Returns and removes (key, value)
```
