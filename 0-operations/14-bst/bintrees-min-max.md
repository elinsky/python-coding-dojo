# Get Min/Max in bintrees

**Q:** How do I get min/max from bintrees BST?

**A:** Use min_item()/max_item() or min_key()/max_key()

```python
t.min_item()  # Returns (key, value) pair
t.max_item()  # Returns (key, value) pair
t.min_key()   # Returns just key
t.max_key()   # Returns just key
```
