# Greedy vs Brute Force Complexity

**Q:** What's typical time improvement from brute-force to greedy?

**A:** Often from O(n²) or O(n!) to O(n log n) (due to sorting) or O(n).

```python
# Brute force: try all pairs/combinations - O(n²) or worse
# Greedy with sort: O(n log n)
# Greedy with one pass: O(n)
```
