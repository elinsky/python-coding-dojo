# Greedy Algorithm Definition

**Q:** What is a greedy algorithm?

**A:** An algorithm that makes locally optimum decisions at each step and never changes those decisions.

```python
# Hallmark: once a decision is made, it's never changed
# Example: selecting coins, making assignments
```
