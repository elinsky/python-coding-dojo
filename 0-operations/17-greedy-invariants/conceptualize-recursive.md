# Conceptualize Recursively, Implement Iteratively

**Q:** What's a good strategy for designing greedy algorithms?

**A:** Conceptualize recursively (easier reasoning), implement iteratively (better performance).

```python
# Think: "What's the greedy choice? Does remaining problem
#         have same structure?"
# Code: Use loops instead of recursion for O(1) space
```
