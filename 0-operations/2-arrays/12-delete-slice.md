# Delete Slice from Array

**Q:** How do I delete a slice from i (inclusive) to j (exclusive)?

**A:** Use del with slice notation - O(n) time

```python
del A[i:j]  # deletes elements from i to j-1 (i inclusive, j exclusive)
```
