# Get Reversed Copy

**Q:** How do I get a reversed copy?

**A:** Use slicing with -1 step or reversed()

```python
A[::-1]           # returns reversed copy
list(reversed(A))  # returns iterator as list
```
