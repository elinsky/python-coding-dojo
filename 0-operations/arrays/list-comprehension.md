# Create Array Using List Comprehension

**Q:** How do I create array using list comprehension?

**A:** [expression for item in iterable if condition]

```python
[x**2 for x in range(10)]       # squares
[x for x in A if x % 2 == 0]    # evens only
```
