# Create Dummy Head Node

**Q:** How do I create a dummy head node?

**A:** Create ListNode with default value, optionally pointing to head

```python
dummy_head = ListNode(0, L)  # or ListNode()
```
