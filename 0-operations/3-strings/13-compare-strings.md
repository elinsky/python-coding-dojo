# Compare Strings Lexicographically

**Q:** How do I compare two strings lexicographically?

**A:** Use comparison operators (<, >, ==, etc.) - O(min(n,m)) time

```python
s1 < s2  # True if s1 comes before s2
s1 == s2  # True if equal
```
