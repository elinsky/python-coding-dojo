# Format String with Placeholders

**Q:** How do I format a string with placeholders?

**A:** Use format() method - O(n) time where n is result length

```python
'Name {name}, Rank {rank}'.format(name='Alice', rank=1)
```
