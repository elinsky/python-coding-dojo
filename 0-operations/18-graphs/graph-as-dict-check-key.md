# Check if Vertex Exists in Graph

**Q:** How do I check if a vertex exists in an adjacency list graph?

**A:** Use 'in' operator to check if key exists in dictionary

```python
if vertex not in graph:
    # vertex doesn't exist in graph
```
