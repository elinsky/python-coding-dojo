# DFS Time Complexity

**Q:** What is the time complexity of DFS on a graph?

**A:** O(|V| + |E|) where V is vertices and E is edges

```python
# We visit each vertex once and
# traverse each edge once
```
