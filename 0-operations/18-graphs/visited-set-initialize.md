# Initialize Visited Set

**Q:** How do I initialize a visited set for graph traversal?

**A:** Create an empty set or set with starting vertex

```python
visited = set()
# or with initial vertex
visited = set([s])
```
