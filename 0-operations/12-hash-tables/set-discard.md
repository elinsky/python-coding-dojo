# Discard Element from Set

**Q:** How do I remove an element from a set (without error if not found)? What is the time complexity?

**A:** Use discard() method (no error if not present). Time: O(1) average

```python
s.discard(x)
```
