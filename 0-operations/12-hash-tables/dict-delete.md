# Delete Key from Dictionary

**Q:** How do I delete a key from a dictionary? What is the time complexity?

**A:** Use del statement (raises KeyError if not found). Time: O(1) average

```python
del d[key]
```
