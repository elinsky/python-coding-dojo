# Remove Element from Set

**Q:** How do I remove an element from a set (with error if not found)? What is the time complexity?

**A:** Use remove() method (raises KeyError if not found). Time: O(1) average

```python
s.remove(x)
```
